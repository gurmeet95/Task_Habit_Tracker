spring:
  application:
    name: Task-Habit-Tracker

  datasource:
    url: jdbc:mysql://localhost:3306/task_habit_db
    username: root
    password: Guru@1069

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true